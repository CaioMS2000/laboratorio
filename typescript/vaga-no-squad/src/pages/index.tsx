import Head from 'next/head'
import Image from 'next/image'
import styles from '@/styles/Home.module.css'
import { useEffect, useState } from "react";
import { AiFillCloseCircle } from 'react-icons/ai';
import { FaPlus, FaMinus } from 'react-icons/fa';

interface User{
  name: string;
  rounds: number;
  id: number;
}

export default function Home() {
  const [users, setUsers] = useState<User[]>([]);

  async function fetchUsers(){
    const res = await fetch('/api/users')
    const data = await res.json()

    setUsers(data)
    console.log(users)
  }

  useEffect(() => {
    fetchUsers()
  }, []);

  return (
    <div className={`${styles.content}`}>
      <Head>
        <title>Comp do LOL</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        {/* <link rel="icon" href="/favicon.ico" /> */}
        <link rel="icon" href="/toplane.png" />

      </Head>
      <h1>CONTROLE DE VAGAS</h1>
      <main className={styles.main}>
        <div className={`${styles.container} container`}>
          <div className={`${styles["players-grid"]}`}>
            <div className={`${styles.row} row`}>
              <div className={`${styles.name} col-8`}>Nick</div>
              <div className={`${styles.rounds} col-4 text-center`}>Vagas</div>
            </div>
            {
              users.map((user, index) => (
                <div key={index} className={`${styles.row} row`}>
                  <div className={`${styles.name} col-8`}>{user.name}</div>
                  <div className={`${styles.rounds} col-2 text-center`}>{user.rounds}</div>
                  <div className={`col-1 d-flex align-items-center justify-content-center`} >
                    <FaMinus />
                  </div>
                  <div className={`col-1 d-flex align-items-center justify-content-center`} >
                    <FaPlus />
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </main>
    </div>
  )
}

// async function handleSubmit(event) {
//   event.preventDefault();
//   const newUser = {
//     name: form.name,
//     email: form.email,
//     phone: form.phone
//   };
//   const response = await fetch('/api/users', {
//     method: 'POST',
//     headers: {
//       'Content-Type': 'application/json'
//     },
//     body: JSON.stringify(newUser)
//   });
//   const data = await response.json();
//   setUsers([...users, data]);
//   setForm({ name: '', email: '', phone: '' });
// }